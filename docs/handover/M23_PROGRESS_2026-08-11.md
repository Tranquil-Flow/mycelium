# M23 progress — heterogeneous stage-local KV

M23 is closed on the physically qualified three-host Qwen2.5-3B route. The route is
`node-0` MLX `[0,22)` → `reviewer-mac-2` MLX `[22,35)` → `node-2` NumPy `[35,36)`
over authenticated Iroh. It ran in one route-wide `stage_local_kv` mode; every stage
performed one prefill operation followed by one-token decode operations and released
its assignment-local KV state to zero.

The fixed-prompt physical A/B used the same model, revision, hosts, stage allocation,
prompt, and token budget. Exact output was `13` in both runs. Replay measured 8609.9 ms
TPOT and 29.27 s total; stage-local KV measured 978.1 ms TPOT and 6.69 s total. Activation
output fell from 5,226,720 to 1,327,272 bytes. The sealed gate is qualified with digest
`sha256:485cc1dc2c0f67ef49373129d401b7fbec4fbf4719219d46ae308408b66df94a`.

Public evidence is the privacy-reduced
`contracts/compatibility-fixtures/m23-kv-gate-v1.json`; owner-only captures remain at
`/Users/evinova-self/mycelium-physical-run/m23-heterogeneous-kv-20260811`. The live UI
polls per-placement architecture, mode support, active/peak KV bytes, position, decode
work, and release reason, and the sealed A/B is exposed by `/__mycelium/m23-kv` when
`deployment/m23-kv-gate.json` is attached.

Dense Qwen3 has a first-class `Qwen3ForCausalLM` adapter with exact tensor ownership,
MLX/NumPy and int8 parity, and stage-local KV coverage. The local Qwen3-8B snapshot is
complete at revision `b968826d9c46dd6066d109eabc6255188de91218`; admission remains
withheld because the current swarm cannot satisfy its contiguous exact memory/disk
allocation. No model was downloaded for M23.

M23 does not claim tensor parallelism, continuous batching, KV migration, or mobile
activation eligibility. Those remain later, separately specified milestones.
