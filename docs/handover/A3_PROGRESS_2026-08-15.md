# A3 Progress — Useful Local Model Qualification

**Gate state:** complete; executed evidence sealed and atomic A3 commit `905786df41ffdad5718d3464733e2f5cb8727532`
**Incumbent:** preserved Qwen2.5-0.5B M4 Pro → Surface deployment
**Candidate:** exact local Qwen2.5-7B revision `a09a35458c702b33eeacc393d103063234e8bc28`
**Authorized representation:** `sha256:c3029635eddc32a7e2f7e8c004a5ab4e96dd4366425a9e10e9e98f5bf2e63d04`

This file is the retained A3 checklist required by the Mycelium completion plan. A3
closed atomically in commit `905786df41ffdad5718d3464733e2f5cb8727532`; current
deployment qualification remains independently fail-closed and may expire later.

## Specification and authority

- [x] Gate-specific implementation specification exists.
- [x] Exact Qwen2.5-7B source revision and source digest are frozen.
- [x] Owner authorization is bound to the exact local source, quantizer, serving
  representation, and no-download decision.
- [x] Qwen3-8B remains catalogued but has no conversion/preparation authority.
- [x] Preparation authorization v2 freezes a start that occurred inside the signed
  capacity lease; activation still requires new live qualification.
- [x] Pin all new A3 cross-process protocols and refresh final source digests.

## Planning and preparation

- [x] Fresh two-Mac signed capacity generation v7 selected contiguous layers `[0,22)`
  and `[22,28)` for the approved representation from direct observations in both
  directions.
- [x] Fresh generation v8 recaptured eight confirmed Router frames in each direction
  over direct M4 Pro ↔ Evi Mac Iroh paths, then generation v9 rebuilt signed one-hour
  resource authority. The exact capacity planner again selected `[0,22)` and
  `[22,28)` without using the expired generation.
- [x] After the late-peer failure, generation v10 recaptured eight physical Iroh
  frames in both directions using the actual activation endpoint identities. Fresh
  one-hour signed resources selected `[0,21)` and `[21,28)` because the Evi host's
  current memory headroom changed; the allocation was not pinned to the prior split.
- [x] Exact product dynamic-program allocation was exercised in a memory-only A/B;
  reducing the second host's fast tier changed the optimum from 22/6 to 24/4 layers.
- [x] Product preparation started from fresh authority with no model download.
- [x] Finish the cold candidate build and its bounded two-token startup challenge from
  the exact local source. The legacy-v1 attempt stopped after the completed build when
  its original capacity lease expired and published no candidate.
- [x] Finish v2 reuse validation, exact cold assignment acquisition, and peer staging
  through the product preparation path.
  The latest v2 attempt copied all 15.24 GB locally but its unattended server process
  ended before a build report or acquisition record was written; no candidate was
  published. A fresh two-member lease is required because both physical peers were
  offline when work resumed on 2026-08-17.
  Fresh authority was recaptured and preparation attempt `944bf7f58fea` reused the
  completed legacy build, materialized and verified the full candidate, and executed
  the bounded two-token streaming startup challenge. It then failed closed before
  plan publication because the Evi Mac became unreachable during the late run-scoped
  peer-identity refresh (`ssh` exit 255). No acquisition, peer staging, activation,
  or incumbent mutation occurred. Evi is now online, and v10 retry attempt
  `1e41172b940b` revalidated the completed exact immutable representation under the
  new 21/7 assignment until the Seagate preparation volume physically disappeared
  from macOS during a shard read on 2026-08-17. The builder exited, the product
  published no candidate, and no acquisition, peer staging, activation, selection,
  download, substitution, or reconversion occurred. The exact attempt remains pending
  a physical remount and a fresh product start bound to that remounted volume. APFS
  copy-on-write materialization preserves private independent inodes and
  falls back to the secure byte-copy path on unsupported filesystems. Reuse now
  permits a fresh planner to change the layer boundary while retaining exact v2
  source-artifact, quantizer, and representation bindings; every file and the startup
  challenge are still revalidated under the new assignment.
  Generation v11 subsequently moved eight fresh direct Iroh frames in each direction
  and current resources changed the exact optimum to `[0,23)` / `[23,28)`. Product
  preparation selected the completed immutable int8 deployment for reuse without a
  download, but was stopped before acquisition or publication when macOS reported
  that the Seagate link had negotiated at only 12 Mb/s. The volume is safely unmounted
  pending a USB 3 reconnect; v11 will not be reused as preparation authority after its
  lease expires.
  The current exact attempt `a09a35458c70-1a7727a2f7e4` subsequently completed and
  durably challenged the 21/7 immutable candidate. Its M4 assignment `[0,21)` reached
  terminal cold promotion as acquisition
  `acquisition-337ae52daf014f7d9c98035bb2f770c9` with 10,878,450,728 origin bytes,
  zero cached bytes, and promotion digest
  `sha256:967d9f6314a7a07aeb0a3ccbc8754c43787813b135815b4b51413fc3556c5775`.
  The server then ended while materializing the Evi assignment `[21,28)`: 346 verified
  12 MiB source objects (about 4.1 GiB) remain in the private attempt and no second
  receipt, peer staging, candidate publication, activation, or selection occurred.
  Preparation remains fail-closed at `seed_bound`; the incumbent was not displaced.
  Product recovery now adopts the one exact legacy source directory into a deterministic
  attempt-local checkpoint, verifies and reuses completed objects by full digest, and
  rejects corrupt, unsafe, symlinked, or ambiguous checkpoints. The focused source and
  preparation recovery suite passes (32 tests). Resumption waits for the physical peer
  to be available; the existing bytes and terminal M4 receipt are preserved.
  Fresh generation v40 subsequently recaptured eight direct Iroh frames in each
  direction, refreshed both signed resource leases, and independently selected the
  same exact `[0,21)` / `[21,28)` placement. Product recovery was accepted under that
  authority with downloads and activation disabled. It reused the challenged build and
  entered assignment acquisition. The M4 content store produced terminal zero-transfer
  receipt `acquisition-1addada109054091837336d195b9aabb`, reporting
  10,878,450,728 cached bytes, zero transferred or origin bytes, and promotion digest
  `sha256:e432c640a9009c0444fb5469686d18e18fe26268609181ff8a39a17036c0f635`.
  Generation v41 then recaptured eight direct Iroh frames in each direction, refreshed
  both signed resource leases, and again independently selected `[0,21)` / `[21,28)`.
  Its first Evi acquisition transferred and verified 187 of 346 chunks
  (2,353,004,544 bytes) with zero origin fallback before failing closed with
  `artifact_chunk_request_ineligible`. The terminal timestamp was 1.1 seconds after the
  transfer grant expired: the grant had been signed before a roughly ten-minute source
  reconciliation, leaving only about five minutes after the remote launch. No partial
  promotion, candidate publication, activation, selection, download, or substitution
  occurred, and the verified remote chunks remain resumable.
  The transport path now timestamps and signs a grant only after source
  materialization/reconciliation, giving the recipient the complete bounded grant
  window. It also validates fresh signed complete availability before rescanning an
  unchanged source manifest, while stale or incomplete availability still forces full
  verification. The focused preparation/transport suite passes 67 tests and Ruff is
  clean. A generation-v41 product recovery is now revalidating the exact immutable
  candidate before resuming the Evi acquisition; this is live progress, not a completed
  cold-acquisition, peer-staging, or qualification claim.
  While that recovery was promoting its new M4 zero-transfer receipt, generation v42
  captured new signed one-hour resource leases on both Macs and reused only the still-
  current generation-v41 physical Iroh observations (eight frames in each direction,
  direct paths). The exact planner again selected `[0,21)` / `[21,28)` for Qwen2.5-7B.
  It also found the existing local Qwen3-8B int8 proposal feasible at `[0,25)` /
  `[25,36)`, with only 64,643,482 bytes of modeled Evi headroom; Qwen3 remains
  unapproved, unprepared, unqualified, and unselectable. Generation v42 is fresh
  authority for the later exact warm reacquisition, not a replacement for the frozen
  generation-v41 cold run.
- [x] Required SSH peers are preflighted before an output root or expensive local
  preparation is created. A passed local startup challenge is durably checkpointed
  before late remote identity refresh. Cold coordinator acquisition storage is now
  preflighted for both object and promoted-file bytes before even scanning the model
  snapshot, while an exact warm terminal acquisition bypasses the cold-space demand.
- [x] Removable preparation and temporary roots are bound to their startup filesystem
  identity. Disappearance or remount/substitution fails closed with the bounded
  `model_preparation_workspace_unavailable` reason, and the catalogue explains the
  recovery in product language while preserving the incumbent.
- [x] Source-manifest reconciliation waits on a byte-scaled, bounded deadline instead
  of a fixed 30 seconds, so a current multi-gigabyte advertisement can finish full
  verification. Expired inbox manifests are now rejected before any model-object hash,
  preventing obsolete retries from repeatedly consuming the preparation volume. The
  focused source/transport suite passes without weakening signed availability checks.
- [x] Repeat acquisition from warm stores and prove zero duplicate transfer.
  Exact warm reacquisition produced one new terminal receipt per stage:
  `acquisition-78df97d4511a4d778351f980e7e81a2b` for `[0,21)` and
  `acquisition-393bdbb256e24e82bea8b4e801454347` for `[21,28)`. Together they
  verified 15,231,271,488 cached bytes with zero transferred bytes and zero origin
  bytes.
- [x] Preserve bounded negative preparation evidence for stale, mismatched,
  unauthorized, incomplete, or unavailable candidates.

## Correctness and physical runtime

- [x] Independent Transformers reference process produced frozen logits for two cases
  and two decode steps without joining the route.
- [x] Assignment-bound split-stage MLX capture passed a real 0.5B harness smoke test.
- [x] Capture the exact challenged 7B candidate logits and pass the predeclared parity
  policy. On 2026-08-18 the assignment-ordered local capture completed both frozen
  cases. Greedy tokens matched, all logits were finite, maximum absolute errors were
  0.904574 and 0.907722 under the frozen 2.0 tolerance, and parity passed as
  `sha256:4b4e8669b16cc83b32a6e3542eee74bda04b5c1ed01870195c32db53a2951ad7`.
  The independent reference process did not join the route, and the capture correctly
  retained `route_ready=false` and `qualified=false`; this is correctness evidence,
  not physical activation or selection authority.
- [x] Activate without displacing the incumbent and pass stage load/startup challenge.
- [x] Prove prefill, multi-token decode, stage-local KV, normal/cancel/shutdown cleanup,
  Router frame movement, and positive work counters on both physical peers.
- [x] Register and explicitly select the currently qualified 7B deployment.
- [x] Complete and retain a browser-streamed 7B request plus terminal history.
  Request `ae347607-373e-4680-a144-f8fb19f463f2` returned “The capital of France
  is Paris.” as seven streamed tokens from the physical 7B route.
- [x] Select the 0.5B deployment again and complete another browser request.
  The final post-negative-gate incumbent request
  `4e716b0c-9949-492b-baf8-39ff1a360a0e` completed with the same seven-token answer.

## Dynamic catalogue and negative selector gate

- [x] Local catalogue exposes compatible models and exact serving operations without a
  fixed model list.
- [x] Prepared candidates have an explicit activate/load lifecycle; preparation alone
  does not make a model selectable.
- [x] A server with configured but unavailable routes remains fail-closed while still
  exposing truthful catalogue and lifecycle controls.
- [x] Expose Qwen3's exact present blocker with zero provisioning. Its compatible local
  identity remains unqualified and unselectable because no exact representation is
  owner-authorized; the previous capacity observation is stale and grants no action.
- [x] Execute selector rejection for infeasible, stale, incomplete, mismatched,
  unqualified, and dead deployments while the incumbent stays usable.
  The A3 sealer now requires all six product-endpoint rejection records, unchanged
  incumbent selection before/after every attempt, and another completed incumbent
  request. Execution remains pending the live qualified route.

## Eight-workspace browser verification

- [x] Inference: qualified-only selector, exact identity/envelope, streaming output,
  activity, and terminal history.
- [x] Device Lab: current eligible capability classes and contribution requirements.
- [x] Network: proposed, selected, and observed order with physical frame movement.
- [x] Nodes: layer ranges, backend, resident/static/KV bytes, artifact/load state.
- [x] Plans: representation decision, exact allocation, memory A/B, transfer scope, and
  rejected alternatives.
- [x] Readiness: separate completeness, authority, feasibility, parity, load,
  qualification, cleanup, and selection rungs.
- [x] Incidents: bounded preparation/load/parity/peer/cleanup failures and incumbent
  preservation.
- [x] Settings: dynamic local catalogue, lifecycle actions, and qualified default.
- [x] Verify refresh, navigation, Back/Forward, reconnect, degraded/stale state,
  terminal history, and a clean second browser session.
  The degraded/stale sub-gate now passes for refresh and Back/Forward: inference stays
  disabled, Qwen2.5-7B shows its approved immutable representation with expired
  capacity, Qwen3-8B shows owner approval required, and tab-session prompt/response
  history survives both refresh and navigation. Positive current-route and clean
  second-session checks remain pending.
  Settings and the Inference/Incidents model projections now distinguish a feasible
  proposed conversion from an approved immutable representation: Qwen3 is labelled
  `Owner approval required`, remains unselectable, and does not imply that preparation
  has started.
  The live Observatory client now also recovers across a backend generation reset only
  after a fresh same-origin snapshot has a strictly newer observation time. A stale
  restart snapshot remains disconnected and fail-closed; the focused regression covers
  both rejection and successful epoch reconstruction.

## Closure

- [x] Focused A3 Python regression currently passes, including the v2 lifecycle,
  durable phase recovery, exact acquired-plan recovery, public-ledger snapshot, and
  resumable source-materialization tests; Ruff passes on the affected files.
- [x] An interim complete Python regression passes on 2026-08-18: 4,032 tests passed,
  13 expected skips, and 121 subtests passed. It must be rerun if the physical evidence
  path requires a source fix.
- [x] After the offline acceptance and local-inspection additions, a fresh complete
  Python regression passes: 4,039 tests, 13 expected skips, and 121 subtests in 520.02
  seconds. It exposed one teardown-only local-socket accept-thread warning in
  `test_network_probe.py`; the test now catches close races, joins the worker, and passes
  20 consecutive warning-as-error repetitions. The physical A3 completion path still
  invalidates this interim result if it requires another source change.
- [x] Current frontend regression passes (88 files, 498 tests), the contract and
  interactive-browser bundle checks pass, TypeScript passes, and the production bundle
  builds after exposing representation authority in product language and adding the
  fail-closed backend-restart reconstruction gate. The live catalogue now describes
  the preparation wait as assigned-artifact verification and missing peer-stage
  acquisition instead of implying that every wait is a local-file scan.
- [x] A device-independent in-app browser smoke on 2026-08-18 visited all eight fixture
  workspaces, preserved the correct workspace through Back/Forward and reload, kept the
  prompt and Start action disabled under non-authoritative fixture evidence, and emitted
  no browser console warnings or errors. This is product-shell/fail-closed evidence only,
  not the pending live two-host browser gate. The standalone three-engine Playwright
  smoke remains an environment prerequisite because its version-pinned Chromium,
  Firefox, and WebKit binaries are not installed; no browser packages were downloaded.
- [x] The live route decoder now accepts the registry's fail-closed
  `configured_deployment_unavailable` incident, with a focused regression; model
  evidence failures are visible instead of silently removing the catalogue.
- [x] Contract fixtures and manifest were regenerated at 67 contracts. Contract,
  governance, governance-gate, claim-boundary (438 source files), and release-security
  (1,095 tracked files) audits pass.
- [x] The governance ledger now explicitly covers both owner-authorized product
  preparation actions, cold start and exact warm reacquisition. Its focused regression
  and governance audit pass; the ledger does not grant activation or selection.
- [x] The post-source-fix complete Python regression passes 4,042 tests, 13 expected
  skips, and 121 subtests. Focused source/transport tests pass 18/18 and Ruff is clean.
- [x] Run final full Python, frontend, contract, governance, privacy, security, and
  accessibility suites after the evidence shape is stable. Final results: 4,191
  Python tests passed, 13 expected skips, and 121 subtests passed; 499 frontend tests
  passed; Chromium, Firefox, and WebKit Playwright smoke passed 3/3; 67 contract
  fixtures passed their audit; governance, governance-gate, claim-boundary, and
  release-security audits reported zero findings; repository-wide Ruff, compileall,
  and `git diff --check` passed.
- [x] Seal `mycelium.a3_useful_model_qualification.v1` from executed artifacts.
  The sealed artifact passes all thirteen checks with evidence digest
  `sha256:11ea9ea9d5d5cb8e057ab02add62715965d22dcf04a7b0c1a62b7ed4a4419ff0`.
- [x] Update the architecture ledger and current handover with observed A3 state.
- [x] Create exactly one atomic A3 feature commit, then advance to A4. This progress
  record is part of that atomic commit; later-gate planning and naming changes remain
  outside it.
